export interface LocationI{
    
    address: string | null,
    apt: string | null,
    city: string | null,
    state: string | null,
    zip: string | null,
    weather: string | null,
    direction: string | null,
    parkingMeterNumber: string | null,
    meterType: string | null,
    zoneType: string | null,
    hasStopLocation: string | null,
    stopLocationAddress: string | null,
    stopLocationApt: string | null,
    stopLocationCity: string | null,
    stopLocationState: string | null,
    stopLocationZip: string | null,
}